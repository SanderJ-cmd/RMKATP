title: "RMKData"
author: "Sander Jegorov"
date: "2026-04-24"
output: html_document

## README

This is an introduction to the analysis of first registration of personal vehicles from Estonia, specifically focusing on the year 2026 in the context of most popular car brands. The dataset is composed of car brands from different manufacturers (BMW,...,Ford) and a number of registrations (1,...,254) corresponding to a year of manufacture (2006,...,2026).

The dataset was taken from the Statistics Estonia website: https://andmed.eesti.ee/datasets/infoleht-%28esmaste-ja-uute-soidukite-statistika%29, where only one sheet of a larger dataset was used to observe a specific proble and older vehicles were disincluded.

The problem is "How many vehicles are first registered from a period of 20 years and how probable it is to choose to register a certain car brand. In this study we'll be focusing on conditional probability, where probability "P" is dependent on "A" brand given "B" year, therefore the notation is following P(A|B). For the probability model we're trying to do we're using the following equation: P(A|B)=P(A∩B)/P(B), which states that "Probability of brand is dependent on B the year of manufacture." To do this for each year separately would be tedius and require a lot of time so therefore we start with firstly taking all of the registered cars together and making a table with different brands.

```{r}
#Reading in the document 

Data <- read.csv(file = "Soidukid.csv")

#Changing data to fit the premise of our analysis better, firstly decreasing the nr of columns and increasing rows. Also corresponding years of registration and brands are now together in the same columns, making the analysis on probability much easier to work with for the whole dataset, not by doing each year separately

library(tidyr)
library(dplyr)
library(ggplot2)

#Creating a modified dataset with extending the rows by removing the multiple year columns and transforming them into one column with repetitions of years with pivot_longer. Mutate is used to transform the year into a numeric line of data.

Data_long <- Data %>%
  pivot_longer(
    cols = starts_with("X20"),
    names_to = "Year",
    values_to = "Count"
  ) %>%
  mutate(
    Year = as.numeric(gsub("X", "", Year))
  )

#Probabilities are calculated with P(A)=Number of times A occurs/Number of total outcomes. This means in this context e.g for year 2026 the first registration of BMW P(A)=71/286 P(A)=0.248 where 71 is the count and 286 the sum in the mutate line
    
Data_year_prob <- Data_long %>%
  group_by(Year) %>%
  mutate(Prob = Count / sum(Count, na.rm = TRUE))

#Plotting the first data with the probability of first registration over the years 2006-2026.This kind of regular xy time series plot doesn't really feel appropriate in this kind of context but still some trends can already be recognized regarding which brand is most commonly registered in each year.

  ggplot(Data_year_prob, aes(x = Year, y = Prob, color = Mark)) +
  geom_line() +
  theme_minimal() +
  geom_point() +
  labs(
    title = "First Registration Probability Over Time",
    x = "Year",
    y = "P(Brand | Year)" 
  )
  
#Therefore for representing this kind of data more accurately a heatmap is made, which creates a better visual of understanding the probabilities of first registration regarding the brand.
  
  ggplot(Data_year_prob, aes(x = Year, y = Mark, fill = Prob)) +
  geom_tile() +
  labs(
    title = "Brand First Registration Probability Heatmap",
    fill = "P(Brand | Year)"
  )

```

The resulting data from the heatmap looks better and can be understood more simply than the first plot. 

To get more in depth I'm also thinking of creating a binomial model for BMW, because of the huge drop of probability, while being the most popular before in terms of first registration in 2026 and we're making it as a case study.

For this a binomial model notation is established: We're expecting a null hypothesis or H~0~to be  

```{r}

```
